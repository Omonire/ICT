# Database (PostgreSQL)
DATABASE_URL=postgresql://postgres:password@localhost:5432/examflow

# Auth
JWT_SECRET=change-me-to-a-random-string
JWT_EXPIRES_IN=7d
COOKIE_SECURE=false

# Backend
PORT=4000
NODE_ENV=development

# Seed demo data on first cold start
SEED_ON_STARTUP=true
